#include<bits/stdc++.h>
using namespace std;
void result(int n){ 
    if(n%2==0) cout<<"Even";
    else cout<<"odd";
}

int main(){ //152
    int n;
    cout<<"Enter the number: ";
    cin>>n;

    result(n);
}