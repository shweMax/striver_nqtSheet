#include<bits/stdc++.h>
using namespace std;
void result(int n){ 
    if(n>=0) cout<<"Positive";
    else cout<<"Negative";
}

int main(){ //152
    int n;
    cout<<"Enter the number: ";
    cin>>n;

    result(n);
}